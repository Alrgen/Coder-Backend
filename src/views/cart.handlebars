<h1>Carrito</h1>

<ul>
    {{#each cart}}
    <li>
        <h2>{{this.product.title}}</h2>
        <span>{{this.product.description}}</span>
        <h3>${{this.product.price}}</h3>
        <h3>Total agregado: {{this.cuantity}}</h3>
    </li>
    {{/each}}
</ul>